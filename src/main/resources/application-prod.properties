# Configuracion de la Base de Datos para PRODUCCION
spring.datasource.url=jdbc:mysql://prod-server:3306/deporuis_prod?useSSL=false&serverTimezone=America/Bogota
spring.datasource.username=deporuis_admin
spring.datasource.password=s3gur0pr0d
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuracion de JPA
spring.jpa.hibernate.ddl-auto=none

# ← leída desde una variable de entorno real
jwt.secret=${JWT_SECRET}

# ← o fallback de 24h
jwt.expiration.ms=${JWT_EXPIRATION_MS:86400000}

